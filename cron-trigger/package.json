{
  "name": "futbol-data-trigger",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Cloudflare Worker: 1-min cron that triggers the futbol live-score GitHub Actions workflow (works around GitHub schedule throttling).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
